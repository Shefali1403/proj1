import React from "react";
const Box3comp = (props) => {
  return (
    <>
      <div className={props.className}>
        <h3>{props.title}</h3>
      </div>
    </>
  );
};
export default Box3comp;
